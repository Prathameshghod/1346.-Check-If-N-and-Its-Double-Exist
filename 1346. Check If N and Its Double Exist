class Solution:
    def checkIfExist(self, arr: List[int]) -> bool:
        for i in arr:
            if 2*i in arr and (i!=0 or arr.count(0)>1):
                return True
        else:
            return False